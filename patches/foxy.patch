diff --git a/src/ros2/rviz/rviz_assimp_vendor/rviz_assimp_vendor-extras.cmake.in b/src/ros2/rviz/rviz_assimp_vendor/rviz_assimp_vendor-extras.cmake.in
index 8e41fe66..4854b903 100644
--- a/src/ros2/rviz/rviz_assimp_vendor/rviz_assimp_vendor-extras.cmake.in
+++ b/src/ros2/rviz/rviz_assimp_vendor/rviz_assimp_vendor-extras.cmake.in
@@ -19,7 +19,7 @@ find_package(assimp REQUIRED QUIET)
 string(REPLACE "/lib/lib/" "/lib/" ASSIMP_LIBRARY_DIRS "${ASSIMP_LIBRARY_DIRS}")
 string(REGEX REPLACE "/lib/include$" "/include" ASSIMP_INCLUDE_DIRS "${ASSIMP_INCLUDE_DIRS}")
 
-set(rviz_assimp_vendor_LIBRARIES)
+set(rviz_assimp_vendor_LIBRARIES assimp::assimp)
 foreach(library IN LISTS ASSIMP_LIBRARIES)
   message(STATUS "library: ${library}")
   if(IS_ABSOLUTE "${library}")
diff --git a/src/ros2/rviz/rviz_common/CMakeLists.txt b/src/ros2/rviz/rviz_common/CMakeLists.txt
index 62bb1091..c9fb1a67 100644
--- a/src/ros2/rviz/rviz_common/CMakeLists.txt
+++ b/rviz_common/CMakeLists.txt
@@ -273,6 +273,7 @@ ament_export_dependencies(
   geometry_msgs
   pluginlib
   rclcpp
+  rviz_assimp_vendor
   sensor_msgs
   std_msgs
   tf2
diff --git a/src/ros2/rviz/rviz_ogre_vendor/CMakeLists.txt b/src/ros2/rviz/rviz_ogre_vendor/CMakeLists.txt
index c400c07f..6ee7601a 100644
--- a/src/ros2/rviz/rviz_ogre_vendor/CMakeLists.txt
+++ b/src/ros2/rviz/rviz_ogre_vendor/CMakeLists.txt
@@ -143,7 +143,7 @@ macro(build_ogre)
       "-DZLIB_ROOT=${CMAKE_CURRENT_BINARY_DIR}/zlib-install")
   endif()
 
-  if(DEFINED CMAKE_TOOLCHAIN_FILE)
+  if(false)
     list(APPEND extra_cmake_args "-DCMAKE_TOOLCHAIN_FILE=${CMAKE_TOOLCHAIN_FILE}")
   else()
     list(APPEND extra_cmake_args "-DCMAKE_C_COMPILER=${CMAKE_C_COMPILER}")
diff --git a/src/ros2/rviz/rviz_ogre_vendor/rviz_ogre_vendor-extras.cmake.in b/src/ros2/rviz/rviz_ogre_vendor/rviz_ogre_vendor-extras.cmake.in
index 0ab09b60..dd59679d 100644
--- a/src/ros2/rviz/rviz_ogre_vendor/rviz_ogre_vendor-extras.cmake.in
+++ b/src/ros2/rviz/rviz_ogre_vendor/rviz_ogre_vendor-extras.cmake.in
@@ -127,10 +127,14 @@ foreach(_lib IN LISTS OGRE_LIBRARIES)
     if(APPLE)
       list(APPEND _extra_interface_link_libraries "-framework Cocoa")
     endif()
-    set_target_properties(rviz_ogre_vendor::OgreMain
-      PROPERTIES
-        "INTERFACE_LINK_LIBRARIES" "${_extra_interface_link_libraries}"
-    )
+    if(CMAKE_VERSION VERSION_LESS 3.11)
+      set_target_properties(rviz_ogre_vendor::OgreMain
+        PROPERTIES
+          "INTERFACE_LINK_LIBRARIES" "${_extra_interface_link_libraries}"
+      )
+    else()
+      target_link_libraries(rviz_ogre_vendor::OgreMain INTERFACE ${_extra_interface_link_libraries})
+    endif()
   endif()
   if("OgreOverlayStatic" STREQUAL ${_lib} OR "OgreOverlay" STREQUAL ${_lib})
     find_library(_ogre_overlay_static_library_abs ${_lib}
